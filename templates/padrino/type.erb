# generated by rpcoder

class API::<%= type.name %>
  attr_reader :<%= type.fields.map {|i| "#{i.name.to_s.to_underscore}" }.join(', :') %>

  def initialize(<%= type.fields.map {|i| "#{i.name.to_s.to_underscore}" }.join(', ') %>)
    <%- type.fields.each do |field| -%>
    @<%= field.name.to_s.to_underscore %> = <%= field.name.to_s.to_underscore %>
    <%- end -%>
  end
end