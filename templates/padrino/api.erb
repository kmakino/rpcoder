# generated by rpcoder

module API
  def self.check_type(type, param, nullable = false)
    if param.nil?
      raise API::RpcValidationException.new('Not enough parameters') unless nullable
    else
      raise API::RpcValidationException.new("Type mismatch: #{param.class} for #{type}") unless param.kind_of?(type)
    end
    return param
  end
end
