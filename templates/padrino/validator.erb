# generated by rpcoder

class API::<%= func.name.to_camelcase %>Validator
  attr_reader :validators

  def initialize(validators)
    @validators = validators
  end

  def validate(params)
    targets = params.symbolize_keys

    <%- func.params.each do |param| -%>
    API::check_type(<%= param.to_ruby_type.id2name %>, targets[:<%= param.name.to_camelcase(false) %>], <%= param.optional? %>)
    <%- end -%>
  end
end
