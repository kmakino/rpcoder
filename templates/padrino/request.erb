# generated by rpcoder

class API::<%= func.name.to_camelcase %>Request
  attr_reader :<%= func.params.map {|i| "#{i.name.to_s.to_underscore}" }.join(', :') %>

  def initialize(params)
    <%- func.params.each do |param| -%>
    @<%= param.name.to_s.to_underscore %> = params[:<%= param.name.to_s.to_camelcase(false) %>]
    <%- end -%>
  end
end