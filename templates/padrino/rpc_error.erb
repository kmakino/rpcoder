# generated by rpcoder

class API::RpcError
  def initialize(errorType, message, targetScene)
    @errorType = errorType
    @message = message
    @targetScene = targetScene
  end

  def response
    [
      :errorType => @errorType
      :message => @message
      :targetScene => @targetScene
    ]
  end
end
